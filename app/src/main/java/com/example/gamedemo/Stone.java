package com.example.gamedemo;

public abstract class Stone extends Entity {
    public Stone(int x, int y, int x_speed, int y_speed, int radius) {
        super(x, y, x_speed, y_speed, radius);
    }
}
